{
  "name": "aria-skills",
  "version": "1.0.0",
  "description": "ARIA framework governed skill registry",
  "type": "module",
  "scripts": {
    "test": "vitest run tests/agents.integration.test.ts"
  },
  "devDependencies": {
    "vitest": "^4.1.6"
  }
}
